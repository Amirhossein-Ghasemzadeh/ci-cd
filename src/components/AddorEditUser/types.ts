export interface IPosition {
  name: string;
  label: string;
}

export interface IInitialValue {
  name: string;
  email: string;
  position: string;
  gender: number;
}

export interface IGender {
  value: number;
  label: string;
}
